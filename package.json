{
  "name": "my-react-parcel",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=development parcel ./public/index.html -d build --port 3000 --open",
    "build": "cross-env NODE_ENV=production parcel build ./public/index.html -d build --public-url /",
    "prebuild": "rimraf build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "react",
    "parcel"
  ],
  "author": "Diego Bonavida",
  "license": "MIT",
  "dependencies": {
    "react": "^16.8.6",
    "react-dom": "^16.8.6"
  },
  "devDependencies": {
    "autoprefixer": "^9.6.1",
    "babel-core": "^6.26.3",
    "babel-plugin-module-resolver": "^3.2.0",
    "babel-preset-env": "^1.7.0",
    "babel-preset-react": "^6.24.1",
    "cross-env": "^5.2.0",
    "node-sass": "^7.0.0",
    "parcel-bundler": "^1.12.3",
    "postcss-modules": "^1.4.1",
    "rimraf": "^2.6.3"
  }
}
